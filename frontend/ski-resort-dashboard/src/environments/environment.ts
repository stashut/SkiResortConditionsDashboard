export const environment = {
  production: false,
  // Empty string = relative URL; the dev server proxy forwards /api and /hubs
  // to http://localhost:5000 (see proxy.conf.json).
  apiBaseUrl: ''
};
